{
  "name": "L8_NodeBase",
  "version": "1.0.0",
  "description": "Лабораторная работа №8 по Node.js",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=development node -r dotenv/config index.js dotenv_config_path=.env.development",
    "build": "NODE_ENV=production node -r dotenv/config index.js dotenv_config_path=.env.production",
    "deploy": "NODE_ENV=domain node -r dotenv/config index.js dotenv_config_path=.env.domain",
    "dev": "nodemon index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "bcryptjs": "^2.4.3",
    "node-fetch": "^2.6.9"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}